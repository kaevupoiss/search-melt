// place files you want to import through the `$lib` alias in this folder.
export type Movie = {
    title: string;
    genre: string;
    release_date: string;
    director: string;
    actor: string;
    rating: number;
    duration: number;
    language: string;
    country: string;
    description: string;
}